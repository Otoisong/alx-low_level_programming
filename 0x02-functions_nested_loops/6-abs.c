#include "main.h"

/**
 * _abs - Computes the absolute value of an intiger.
 * @n: The intiger to be computed.
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (0);
	else
		return (-n);
}
