#include <stdio.h>
/**
 * main - Print the name of program
 * @argc: number of argument past to the function
 * @argv: the vector of the argument past to the prgram
 *
 * Return: Always 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
